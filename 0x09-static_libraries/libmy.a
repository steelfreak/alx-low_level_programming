#include <stdio.h>
#include "main.h"

/**
 * main - prints the name followed by a new line
  *@argc: arguments count
  *@argv: arguments vector
  *Return: Always 0 (Success)
  */

int main(int argc _attribute((_unused_)), char **argv)
{
	intf("%s\n", argv[0]);
        return (0);
}